export enum tagTypes {
    user = "user",
    service = "service",
    category= "category",
    booking= "booking"
  }
  
  export const tagTypesList = [
    tagTypes.user,
    tagTypes.service,
    tagTypes.category,
    tagTypes.booking
  ];